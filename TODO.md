# TODO: Update Size Chart Table in AddProduct.jsx

## Steps to Complete:
- [ ] Modify the `addSizeChartToDescription` function to generate an HTML table with 5px solid border instead of markdown.
- [ ] Add a description preview section below the textarea that renders the description as HTML using `dangerouslySetInnerHTML`.
- [ ] Update the inline styles in the size chart editor preview table to use 5px solid border for consistency.
